# Fixtures

`observations_list.json` is **SYNTHETIC** — shaped from the prior-art repo's
field usage (docs/API.md), including the real scope's bare-NaN quirk. Replace
with a recorded response from the real scope (scrub serial numbers/SSIDs) as
soon as one is captured; keep the NaN.
