# README images

The `.svg` files here are **placeholders** — swap them for real captures and the
main README picks them up automatically (same filenames):

| File | Replace with |
|---|---|
| `cli-pull.svg` | A screenshot of `scopepull pull` running (the live progress line) |
| `ddd-toggle.svg` | A screenshot of the Unistellar app's Direct Data Download toggle |
| `result.svg` | An image you stacked from the raw frames scopepull pulled |
| `hero.svg` | Optional: a nicer banner / a photo of your scope |

Drop in a PNG/JPG and either keep the `.svg` name in the README or update the
link. Recommended width ~1000px.
